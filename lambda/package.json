{
  "name": "track-metadata-lambda",
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "package:metadata": "npm run build && cd dist && cp -r ../node_modules . && zip -r ../fetchMetadata.zip fetchMetadata.js node_modules",
    "package:duration": "npm run build && cd dist && cp -r ../node_modules . && zip -r ../extractDuration.zip extractDuration.js node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "dotenv": "^17.4.2",
    "music-metadata": "^7.14.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
